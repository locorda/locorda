import 'package:test/test.dart';

import '../07_content_types.dart' as app;

void main() {
  test('07_content_types example runs without errors', () {
    expect(() => app.main(), returnsNormally);
  });
}
