import 'package:test/test.dart';

import '../03_create_graphs.dart' as app;

void main() {
  test('03_create_graphs example runs without errors', () {
    expect(() => app.main(), returnsNormally);
  });
}
