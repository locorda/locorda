import 'package:test/test.dart';
import '../05_query_update.dart' as app;

void main() {
  test('05_query_update example runs without errors', () {
    expect(() => app.main(), returnsNormally);
  });
}
