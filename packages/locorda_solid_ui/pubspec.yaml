name: locorda_ui
description: Flutter UI components for locorda including login forms and sync widgets
version: 0.9.15-dev
homepage: https://kkalass.github.io/locorda/
repository: https://github.com/kkalass/locorda
issue_tracker: https://github.com/kkalass/locorda/issues
publish_to: none

topics:
  - solid
  - flutter
  - crdt

environment:
  sdk: ^3.6.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  locorda_core:
    path: ../locorda_core
  locorda_solid_auth:
    path: ../locorda_solid_auth

  # Logging
  logging: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ">=5.1.1 <7.0.0"