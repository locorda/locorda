name: vocab_metadata_extractor
description: Extracts metadata from RDF vocabularies and mappings for the Locorda website
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  locorda_rdf_core: ^0.12.0
  locorda_rdf_terms_core: ^0.12.0
  path: ^1.9.0
