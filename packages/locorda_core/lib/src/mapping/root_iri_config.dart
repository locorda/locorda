class RootIriConfig {
  final String fragment;
  // here we could add configuration options if needed in the future
  // for example naming strategies etc.
  const RootIriConfig([this.fragment = 'it']);
}
